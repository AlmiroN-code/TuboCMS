# Система каналов RexTube - Расширенная версия

## Обзор

Система каналов RexTube теперь включает полноценную экосистему для создателей контента, включающую:
- **Базовые каналы** - создание и управление каналами трех типов
- **Плейлисты** - организация контента в тематические коллекции
- **Аналитика** - детальная статистика и метрики для владельцев каналов
- **Монетизация** - система донатов и Premium подписок
- **Брендинг** - кастомизация внешнего вида каналов

## Новые возможности

### 🎵 Система плейлистов
- Создание тематических плейлистов для организации видео
- 4 уровня видимости: публичные, скрытые, приватные, Premium
- Управление порядком видео с drag & drop
- Автовоспроизведение плейлистов
- Навигация между видео в плейлисте
- Статистика просмотров плейлистов

### 📊 Расширенная аналитика
- **Дашборд** с ключевыми метриками (просмотры, подписчики, доходы, время просмотра)
- **Графики** динамики по дням/месяцам
- **Демографические данные** аудитории (возраст, пол, страны)
- **Источники трафика** (прямые, поиск, соцсети, рефералы)
- **Топ контент** за выбранный период
- **Сравнительная аналитика** с предыдущими периодами
- **Экспорт данных** в CSV формате
- **Реальное время** для мониторинга текущей активности

### 💰 Система донатов
- Прием донатов от зрителей с сообщениями
- Анонимные и публичные донаты
- Статистика донатов и топ донатеры
- Интеграция с аналитикой каналов
- Уведомления о новых донатах

### 🎨 Улучшенный брендинг
- Кастомные цвета для каналов
- Аватары и баннеры каналов
- Социальные ссылки (Twitter, Instagram, OnlyFans)
- Контактная информация

## Структура базы данных

### Новые таблицы

#### `channel_analytics`
```sql
- id (INT, PRIMARY KEY)
- channel_id (INT, FK) - канал
- date (DATE) - дата аналитики
- views (INT) - просмотры за день
- unique_views (INT) - уникальные просмотры
- new_subscribers (INT) - новые подписчики
- unsubscribers (INT) - отписки
- likes (INT) - лайки
- comments (INT) - комментарии
- shares (INT) - поделились
- revenue (DECIMAL) - доходы
- watch_time_minutes (INT) - время просмотра в минутах
- demographic_data (JSON) - демографические данные
- traffic_sources (JSON) - источники трафика
- created_at (DATETIME)
```

#### `channel_playlists`
```sql
- id (INT, PRIMARY KEY)
- channel_id (INT, FK) - канал
- title (VARCHAR) - название плейлиста
- slug (VARCHAR, UNIQUE) - URL-slug
- description (TEXT) - описание
- thumbnail (VARCHAR) - превью плейлиста
- visibility (ENUM) - видимость (public/unlisted/private/premium)
- is_active (BOOLEAN) - активность
- videos_count (INT) - количество видео
- views_count (INT) - просмотры плейлиста
- sort_order (INT) - порядок сортировки
- created_at/updated_at (DATETIME)
```

#### `playlist_videos`
```sql
- id (INT, PRIMARY KEY)
- playlist_id (INT, FK) - плейлист
- video_id (INT, FK) - видео
- sort_order (INT) - порядок в плейлисте
- added_at (DATETIME) - дата добавления
- added_by_id (INT, FK) - кто добавил
```

#### `channel_donations`
```sql
- id (INT, PRIMARY KEY)
- channel_id (INT, FK) - канал
- donor_id (INT, FK) - донатер
- amount (DECIMAL) - сумма
- currency (VARCHAR) - валюта
- message (TEXT) - сообщение
- is_anonymous (BOOLEAN) - анонимный донат
- status (ENUM) - статус (pending/completed/failed/refunded)
- payment_id (VARCHAR) - ID платежа
- payment_method (VARCHAR) - способ оплаты
- fee (DECIMAL) - комиссия
- net_amount (DECIMAL) - чистая сумма
- created_at (DATETIME)
- processed_at (DATETIME)
- metadata (JSON) - дополнительные данные
```

## Файловая структура

### Новые сущности
- `src/Entity/ChannelAnalytics.php` - аналитика каналов
- `src/Entity/ChannelPlaylist.php` - плейлисты каналов
- `src/Entity/PlaylistVideo.php` - связь плейлистов и видео
- `src/Entity/ChannelDonation.php` - донаты каналов

### Новые репозитории
- `src/Repository/ChannelAnalyticsRepository.php` - запросы аналитики
- `src/Repository/ChannelPlaylistRepository.php` - запросы плейлистов
- `src/Repository/PlaylistVideoRepository.php` - управление видео в плейлистах
- `src/Repository/ChannelDonationRepository.php` - запросы донатов

### Новые сервисы
- `src/Service/ChannelAnalyticsService.php` - бизнес-логика аналитики
- `src/Service/PlaylistService.php` - управление плейлистами

### Новые контроллеры
- `src/Controller/PlaylistController.php` - публичные страницы плейлистов
- `src/Controller/ChannelAnalyticsController.php` - аналитика каналов

### Новые шаблоны
- `templates/playlist/` - шаблоны плейлистов
  - `index.html.twig` - список плейлистов
  - `show.html.twig` - страница плейлиста
  - `create.html.twig` - создание плейлиста
  - `edit.html.twig` - редактирование плейлиста
- `templates/channel/analytics/` - шаблоны аналитики
  - `dashboard.html.twig` - основной дашборд
  - `demographics.html.twig` - демографические данные
  - `revenue.html.twig` - доходы и донаты
  - `realtime.html.twig` - данные в реальном времени

## URL-маршруты

### Плейлисты
- `GET /playlists` - список всех плейлистов
- `GET /playlist/{slug}` - страница плейлиста
- `GET /channel/{slug}/playlists` - плейлисты канала
- `GET /my-playlists` - мои плейлисты (авторизация)
- `GET /playlist/create` - создать плейлист
- `POST /playlist/create` - обработка создания
- `GET /playlist/{slug}/edit` - редактировать плейлист
- `POST /playlist/{slug}/add-video` - добавить видео (AJAX)
- `POST /playlist/{slug}/remove-video` - удалить видео (AJAX)
- `POST /playlist/{slug}/move-video` - переместить видео (AJAX)
- `POST /playlist/{slug}/delete` - удалить плейлист

### Аналитика каналов
- `GET /channel/{slug}/analytics` - дашборд аналитики
- `GET /channel/{slug}/analytics/demographics` - демографические данные
- `GET /channel/{slug}/analytics/revenue` - доходы
- `GET /channel/{slug}/analytics/export` - экспорт данных
- `GET /channel/{slug}/analytics/real-time` - реальное время
- `GET /channel/{slug}/analytics/views-chart` - данные для графиков (AJAX)
- `GET /channel/{slug}/analytics/api/realtime-data` - API реального времени

## Основные методы API

### ChannelAnalyticsService
```php
// Запись событий
recordView(Channel $channel, ?User $user, array $metadata): void
recordSubscription(Channel $channel, User $user, bool $isNew): void
recordLike(Channel $channel): void
recordComment(Channel $channel): void
recordRevenue(Channel $channel, float $amount): void

// Получение данных
getChannelDashboard(Channel $channel, int $days): array
getComparativeAnalytics(Channel $channel, int $days): array
exportAnalytics(Channel $channel, DateTimeInterface $start, DateTimeInterface $end): string
```

### PlaylistService
```php
// Управление плейлистами
createPlaylist(Channel $channel, string $title, ?string $description, string $visibility): ChannelPlaylist
updatePlaylist(ChannelPlaylist $playlist, ?string $title, ?string $description, ?string $visibility): void
deletePlaylist(ChannelPlaylist $playlist): void

// Управление видео в плейлистах
addVideoToPlaylist(ChannelPlaylist $playlist, Video $video, ?User $addedBy): PlaylistVideo
removeVideoFromPlaylist(ChannelPlaylist $playlist, Video $video): void
moveVideoInPlaylist(ChannelPlaylist $playlist, Video $video, int $newPosition): void

// Получение данных
getChannelPlaylists(Channel $channel, ?User $viewer, int $limit, int $offset): array
getPlaylistVideos(ChannelPlaylist $playlist, int $limit, int $offset): array
getPlaylistNavigation(ChannelPlaylist $playlist, Video $video): ?array

// Проверка прав
canUserManagePlaylist(ChannelPlaylist $playlist, ?User $user): bool
canUserViewPlaylist(ChannelPlaylist $playlist, ?User $user): bool
```

## Интеграция с существующей системой

### Обновленная навигация
- Добавлена ссылка "Плейлисты" в главное меню
- В пользовательском меню добавлены "Мои плейлисты" и "Аналитика"

### Интеграция с видео
- На странице видео добавлена навигация по плейлисту (если видео в плейлисте)
- Кнопка "Добавить в плейлист" на карточках видео
- Автовоспроизведение следующего видео в плейлисте

### Интеграция с каналами
- На странице канала отображаются плейлисты
- Владельцы каналов имеют доступ к аналитике
- Кнопка "Поддержать" для донатов на странице канала

## Права доступа и безопасность

### Плейлисты
- **Создание**: только авторизованные пользователи с каналами
- **Управление**: только владельцы каналов и администраторы
- **Просмотр**: зависит от настроек видимости плейлиста
- **Premium контент**: только для пользователей с Premium статусом

### Аналитика
- **Доступ**: только владельцы каналов и администраторы
- **Экспорт данных**: только владельцы каналов
- **Реальное время**: обновление каждые 30 секунд

### Донаты
- **Отправка**: только авторизованные пользователи
- **Получение**: владельцы активных каналов
- **Анонимность**: по выбору донатера

## Монетизация и бизнес-модель

### Premium подписки
- Доступ к Premium плейлистам
- Расширенная аналитика
- Приоритетная обработка видео
- Отсутствие рекламы

### Система донатов
- Комиссия платформы: 5-10%
- Минимальная сумма: $1
- Максимальная сумма: $1000
- Поддержка различных валют

### Монетизация каналов
- Платные подписки на каналы
- Эксклюзивный контент для подписчиков
- Система чаевых и донатов
- Партнерская программа для популярных каналов

## Производительность и масштабирование

### Оптимизации
- Индексы для быстрого поиска плейлистов и аналитики
- Кэширование популярных плейлистов
- Пагинация для больших списков
- Ленивая загрузка превью плейлистов

### Аналитика
- Агрегация данных по дням для быстрых запросов
- Предварительный расчет популярных метрик
- Кэширование графиков и дашбордов
- Асинхронная обработка событий аналитики

## Будущие улучшения

### Планируемые функции
- **Коллаборативные плейлисты** - совместное управление плейлистами
- **Умные плейлисты** - автоматическое добавление видео по критериям
- **Расширенная аналитика** - A/B тестирование, воронки конверсии
- **Интеграция с соцсетями** - автопостинг новых видео
- **Мобильное приложение** - нативные приложения для iOS/Android
- **API для разработчиков** - открытый API для интеграций
- **Система рекомендаций** - ML-алгоритмы для персонализации

### Технические улучшения
- **Микросервисная архитектура** - выделение аналитики в отдельный сервис
- **Elasticsearch** - для быстрого поиска по плейлистам
- **Redis** - для кэширования и сессий
- **CDN** - для быстрой доставки контента
- **Мониторинг** - детальный мониторинг производительности

## Заключение

Расширенная система каналов RexTube теперь предоставляет полноценную экосистему для создателей контента, включающую все необходимые инструменты для:
- Организации контента в плейлисты
- Детальной аналитики и мониторинга
- Монетизации через донаты и подписки
- Брендинга и кастомизации каналов
- Взаимодействия с аудиторией

Система спроектирована с учетом масштабируемости и может легко расширяться новыми функциями по мере роста платформы.

## Структура базы данных

### Таблица `channels`
```sql
- id (INT, PRIMARY KEY)
- name (VARCHAR(255)) - название канала
- slug (VARCHAR(255), UNIQUE) - URL-slug
- description (TEXT) - описание канала
- type (VARCHAR(20)) - тип канала (personal/studio/company)
- avatar (VARCHAR(255)) - аватар канала
- banner (VARCHAR(255)) - баннер канала
- primary_color (VARCHAR(7)) - основной цвет брендинга
- secondary_color (VARCHAR(7)) - дополнительный цвет
- website (VARCHAR(255)) - веб-сайт
- email (VARCHAR(255)) - контактный email
- twitter (VARCHAR(100)) - Twitter username
- instagram (VARCHAR(100)) - Instagram username
- onlyfans (VARCHAR(100)) - OnlyFans username
- is_verified (BOOLEAN) - статус верификации
- is_active (BOOLEAN) - активность канала
- is_premium (BOOLEAN) - Premium статус
- subscription_price (DECIMAL(10,2)) - цена подписки
- subscribers_count (INT) - количество подписчиков
- videos_count (INT) - количество видео
- total_views (BIGINT) - общее количество просмотров
- created_at (DATETIME) - дата создания
- updated_at (DATETIME) - дата обновления
- owner_id (INT, FK) - владелец канала
```

### Таблица `channel_subscriptions`
```sql
- id (INT, PRIMARY KEY)
- user_id (INT, FK) - подписчик
- channel_id (INT, FK) - канал
- is_paid (BOOLEAN) - платная подписка
- paid_until (DATETIME) - действует до
- notifications_enabled (BOOLEAN) - уведомления включены
- subscribed_at (DATETIME) - дата подписки
```

### Таблица `channel_members`
```sql
- id (INT, PRIMARY KEY)
- user_id (INT, FK) - участник
- channel_id (INT, FK) - канал
- role (VARCHAR(20)) - роль (member/moderator/admin/owner)
- is_active (BOOLEAN) - активность участника
- joined_at (DATETIME) - дата присоединения
- invited_by_id (INT, FK) - кто пригласил
```

### Связь с видео
В таблицу `video` добавлено поле `channel_id` для привязки видео к каналу.

## Файловая структура

### Сущности (Entities)
- `src/Entity/Channel.php` - основная сущность канала
- `src/Entity/ChannelSubscription.php` - подписки на каналы
- `src/Entity/ChannelMember.php` - участники каналов

### Репозитории (Repositories)
- `src/Repository/ChannelRepository.php` - запросы для каналов
- `src/Repository/ChannelSubscriptionRepository.php` - запросы для подписок
- `src/Repository/ChannelMemberRepository.php` - запросы для участников

### Контроллеры (Controllers)
- `src/Controller/ChannelController.php` - публичные страницы каналов
- `src/Controller/Admin/AdminChannelController.php` - админ-панель каналов

### Сервисы (Services)
- `src/Service/ChannelService.php` - бизнес-логика каналов

### Шаблоны (Templates)
- `templates/channel/` - публичные шаблоны
  - `index.html.twig` - список каналов
  - `show.html.twig` - страница канала
  - `studios.html.twig` - список студий
  - `verified.html.twig` - верифицированные каналы
  - `create.html.twig` - создание канала
  - `my_channels.html.twig` - мои каналы
- `templates/admin/channels/` - админские шаблоны
  - `index.html.twig` - список в админке
  - `create.html.twig` - создание в админке
  - `edit.html.twig` - редактирование в админке

## URL-маршруты

### Публичные маршруты
- `GET /channels` - список всех каналов
- `GET /channels/studios` - список студий
- `GET /channels/verified` - верифицированные каналы
- `GET /channel/{slug}` - страница канала
- `GET /channel/{slug}/about` - информация о канале
- `POST /channel/{slug}/subscribe` - подписаться на канал
- `POST /channel/{slug}/unsubscribe` - отписаться от канала
- `GET /my-channels` - мои каналы (требует авторизации)
- `GET /create-channel` - создать канал (требует авторизации)
- `POST /create-channel` - обработка создания канала

### Административные маршруты
- `GET /admin/channels` - список каналов в админке
- `GET /admin/channels/create` - создать канал в админке
- `POST /admin/channels/create` - обработка создания
- `GET /admin/channels/{id}/edit` - редактировать канал
- `POST /admin/channels/{id}/edit` - обработка редактирования
- `POST /admin/channels/{id}/delete` - удалить канал
- `POST /admin/channels/{id}/toggle-verification` - изменить верификацию
- `POST /admin/channels/{id}/toggle-active` - изменить активность

## Основные методы API

### ChannelService
```php
// Создание канала
createChannel(User $owner, string $name, string $description = null, string $type = 'personal'): Channel

// Подписка на канал
subscribeToChannel(User $user, Channel $channel): ChannelSubscription

// Отписка от канала
unsubscribeFromChannel(User $user, Channel $channel): void

// Проверка подписки
isUserSubscribed(User $user, Channel $channel): bool

// Обновление статистики
updateChannelStats(Channel $channel): void

// Поиск каналов
searchChannels(string $query, array $filters = [], int $limit = 20, int $offset = 0): array

// Получение каналов пользователя
getUserChannels(User $user): array

// Проверка прав управления
canUserManageChannel(User $user, Channel $channel): bool
```

### ChannelRepository
```php
// Поиск активных каналов
findActive(int $limit = 20, int $offset = 0): array

// Поиск по slug
findBySlug(string $slug): ?Channel

// Поиск по владельцу
findByOwner(User $owner): array

// Популярные каналы
findPopular(int $limit = 20, int $offset = 0): array

// Верифицированные каналы
findVerified(int $limit = 20, int $offset = 0): array

// Студии
findStudios(int $limit = 20, int $offset = 0): array

// Поиск с фильтрами
findWithFilters(array $filters, int $limit = 20, int $offset = 0): array
```

## Медиафайлы

### Структура папок
```
public/media/channels/
├── avatars/     # Аватары каналов
└── banners/     # Баннеры каналов
```

### Методы для URL медиафайлов
```php
// В сущности Channel
getAvatarUrl(): ?string  // Возвращает /media/channels/avatars/{filename}
getBannerUrl(): ?string  // Возвращает /media/channels/banners/{filename}
```

## Интеграция с навигацией

### Главное меню
Добавлена ссылка "Каналы" в главную навигацию сайта между "Модели" и "Сообщество".

### Пользовательское меню
В выпадающем меню пользователя добавлена ссылка "Мои каналы".

### Админ-панель
В сайдбаре админ-панели добавлена секция "Channels" после секции "Publications".

## Переводы

Все тексты интерфейса переведены на русский язык в файле `translations/messages.ru.yaml`:
- Навигация (`nav.channels`)
- Пользовательское меню (`auth.my_channels`)
- Интерфейс каналов (`channel.*`)

## Права доступа

### Создание каналов
- Требуется авторизация (`ROLE_USER`)
- Любой зарегистрированный пользователь может создать канал

### Управление каналами
- Владелец канала может редактировать свой канал
- Администраторы (`ROLE_ADMIN`) могут управлять любыми каналами

### Подписки
- Требуется авторизация для подписки/отписки
- Нельзя подписаться на свой собственный канал

## Статистика и аналитика

### Счетчики каналов
- `subscribers_count` - количество подписчиков
- `videos_count` - количество видео
- `total_views` - общее количество просмотров всех видео канала

### Автоматическое обновление
Статистика обновляется автоматически при:
- Подписке/отписке пользователей
- Публикации новых видео
- Просмотре видео канала

## Особенности реализации

### Уникальность slug
При создании канала автоматически генерируется уникальный slug на основе названия. Если slug уже существует, добавляется числовой суффикс.

### Типы каналов
Поддерживаются три типа каналов с различным отображением в интерфейсе:
- Личные каналы (синий цвет)
- Студии (фиолетовый цвет)  
- Компании (зеленый цвет)

### Верификация
Каналы могут быть верифицированы администраторами. Верифицированные каналы отображаются с синей галочкой.

### Premium подписки
Каналы могут быть Premium с платной подпиской. Цена указывается в долларах США.

### Брендинг
Каналы могут настраивать:
- Основной и дополнительный цвета
- Аватар и баннер
- Социальные ссылки (Twitter, Instagram, OnlyFans)
- Контактную информацию

## Миграции

Система каналов добавлена миграцией `Version20260123214046.php`, которая создает:
- Таблицу `channels`
- Таблицу `channel_subscriptions` 
- Таблицу `channel_members`
- Поле `channel_id` в таблице `video`
- Соответствующие индексы и внешние ключи

## Будущие улучшения

### Планируемые функции
- Загрузка аватаров и баннеров через веб-интерфейс
- Система ролей и разрешений для участников каналов
- Уведомления о новых видео на каналах
- Аналитика для владельцев каналов
- Монетизация и платные подписки
- API для мобильных приложений
- Интеграция с внешними платформами

### Оптимизации
- Кэширование популярных каналов
- Индексы для быстрого поиска
- Пагинация для больших списков
- Ленивая загрузка изображений