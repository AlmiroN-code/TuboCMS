{% extends 'members/profile/base.html.twig' %}

{% block page_title %}{{ 'profile.tabs.bookmarks'|trans }}{% endblock %}

{% block tab_content %}
<div class="space-y-6">
    <!-- Header -->
    <div class="flex items-center justify-between">
        <h2 class="text-xl font-semibold text-gray-900">
            {{ 'profile.bookmarks.my_bookmarks'|trans }}
            <span class="text-sm font-normal text-gray-500">{{ 'profile.bookmarks.total_bookmarks'|trans({'%count%': total_bookmarks}) }}</span>
        </h2>
    </div>

    <!-- Bookmarks Grid -->
    {% if bookmarks|length > 0 %}
        <div class="video-grid">
            {% for bookmark in bookmarks %}
                {% include 'video/_card.html.twig' with {'video': bookmark.video} %}
            {% endfor %}
        </div>

        <!-- Pagination -->
        {% if total_pages > 1 %}
            <div class="flex justify-center">
                <nav class="flex items-center gap-2">
                    {% if current_page > 1 %}
                        <a href="{{ path('user_profile_bookmarks', {'username': user.username, 'page': current_page - 1}) }}" 
                           class="px-3 py-2 text-sm font-medium text-gray-500 bg-white border border-gray-300 rounded-md hover:bg-gray-50">
                            {{ 'pagination.previous'|trans }}
                        </a>
                    {% endif %}

                    {% for page in range(max(1, current_page - 2), min(total_pages, current_page + 2)) %}
                        {% if page == current_page %}
                            <span class="px-3 py-2 text-sm font-medium text-white bg-blue-600 border border-blue-600 rounded-md">
                                {{ page }}
                            </span>
                        {% else %}
                            <a href="{{ path('user_profile_bookmarks', {'username': user.username, 'page': page}) }}" 
                               class="px-3 py-2 text-sm font-medium text-gray-500 bg-white border border-gray-300 rounded-md hover:bg-gray-50">
                                {{ page }}
                            </a>
                        {% endif %}
                    {% endfor %}

                    {% if current_page < total_pages %}
                        <a href="{{ path('user_profile_bookmarks', {'username': user.username, 'page': current_page + 1}) }}" 
                           class="px-3 py-2 text-sm font-medium text-gray-500 bg-white border border-gray-300 rounded-md hover:bg-gray-50">
                            {{ 'pagination.next'|trans }}
                        </a>
                    {% endif %}
                </nav>
            </div>
        {% endif %}
    {% else %}
        <div class="text-center py-12">
            <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 5a2 2 0 012-2h10a2 2 0 012 2v16l-7-3.5L5 21V5z"></path>
            </svg>
            <h3 class="mt-2 text-sm font-medium text-gray-900">{{ 'profile.bookmarks.no_bookmarks'|trans }}</h3>
            <p class="mt-1 text-sm text-gray-500">{{ 'profile.bookmarks.no_bookmarks_description'|trans }}</p>
        </div>
    {% endif %}
</div>
{% endblock %}