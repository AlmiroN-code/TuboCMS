{% extends 'admin/base.html.twig' %}

{% block title %}Dashboard{% endblock %}
{% block page_title %}Dashboard{% endblock %}

{% block content %}
{# Quick Actions #}
<div class="bg-white rounded-md shadow p-4 mb-6">
    <h3 class="text-sm font-semibold text-gray-900 mb-3">⚡ Быстрые действия</h3>
    <div class="flex flex-wrap gap-2">
        <a href="{{ path('admin_videos_new') }}" class="inline-flex items-center px-3 py-1.5 bg-blue-600 text-white text-sm font-medium rounded-md hover:bg-blue-700 transition-colors">
            <svg class="w-4 h-4 mr-1.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"></path>
            </svg>
            Добавить видео
        </a>
        <a href="{{ path('admin_users_new') }}" class="inline-flex items-center px-3 py-1.5 bg-green-600 text-white text-sm font-medium rounded-md hover:bg-green-700 transition-colors">
            <svg class="w-4 h-4 mr-1.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18 9v3m0 0v3m0-3h3m-3 0h-3m-2-5a4 4 0 11-8 0 4 4 0 018 0zM3 20a6 6 0 0112 0v1H3v-1z"></path>
            </svg>
            Добавить пользователя
        </a>
        <a href="{{ path('admin_categories_new') }}" class="inline-flex items-center px-3 py-1.5 bg-purple-600 text-white text-sm font-medium rounded-md hover:bg-purple-700 transition-colors">
            <svg class="w-4 h-4 mr-1.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 7v10a2 2 0 002 2h14a2 2 0 002-2V9a2 2 0 00-2-2h-6l-2-2H5a2 2 0 00-2 2z"></path>
            </svg>
            Добавить категорию
        </a>
        <a href="{{ path('admin_settings') }}" class="inline-flex items-center px-3 py-1.5 bg-gray-600 text-white text-sm font-medium rounded-md hover:bg-gray-700 transition-colors">
            <svg class="w-4 h-4 mr-1.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10.325 4.317c.426-1.756 2.924-1.756 3.35 0a1.724 1.724 0 002.573 1.066c1.543-.94 3.31.826 2.37 2.37a1.724 1.724 0 001.065 2.572c1.756.426 1.756 2.924 0 3.35a1.724 1.724 0 00-1.066 2.573c.94 1.543-.826 3.31-2.37 2.37a1.724 1.724 0 00-2.572 1.065c-.426 1.756-2.924 1.756-3.35 0a1.724 1.724 0 00-2.573-1.066c-1.543.94-3.31-.826-2.37-2.37a1.724 1.724 0 00-1.065-2.572c-1.756-.426-1.756-2.924 0-3.35a1.724 1.724 0 001.066-2.573c-.94-1.543.826-3.31 2.37-2.37.996.608 2.296.07 2.572-1.065z"></path>
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
            </svg>
            Настройки
        </a>
    </div>
</div>
{# Основные карточки статистики #}
<div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-4 mb-6">
    {# Видео #}
    <div class="bg-white rounded-md shadow p-4">
        <div class="flex items-center justify-between">
            <div class="p-2 bg-blue-100 ">
                <svg class="w-5 h-5 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 10l4.553-2.276A1 1 0 0121 8.618v6.764a1 1 0 01-1.447.894L15 14M5 18h8a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v8a2 2 0 002 2z"></path>
                </svg>
            </div>
            {% if stats.videos_today > 0 %}
            <span class="text-xs font-medium text-green-600 bg-green-100 px-2 py-0.5 rounded-full">+{{ stats.videos_today }} today</span>
            {% endif %}
        </div>
        <p class="text-2xl font-bold text-gray-900 mt-2">{{ stats.total_videos|number_format }}</p>
        <p class="text-xs text-gray-500">Видео</p>
    </div>

    {# Пользователи #}
    <div class="bg-white rounded-md shadow p-4">
        <div class="flex items-center justify-between">
            <div class="p-2 bg-pink-100 ">
                <svg class="w-5 h-5 text-pink-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197M13 7a4 4 0 11-8 0 4 4 0 018 0z"></path>
                </svg>
            </div>
            {% if stats.users_today > 0 %}
            <span class="text-xs font-medium text-green-600 bg-green-100 px-2 py-0.5 rounded-full">+{{ stats.users_today }} today</span>
            {% endif %}
        </div>
        <p class="text-2xl font-bold text-gray-900 mt-2">{{ stats.total_users|number_format }}</p>
        <p class="text-xs text-gray-500">Пользователи</p>
    </div>

    {# Просмотры #}
    <div class="bg-white rounded-md shadow p-4">
        <div class="flex items-center justify-between">
            <div class="p-2 bg-green-100 ">
                <svg class="w-5 h-5 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path>
                </svg>
            </div>
        </div>
        <p class="text-2xl font-bold text-gray-900 mt-2">{{ stats.total_views|number_format }}</p>
        <p class="text-xs text-gray-500">Просмотры</p>
    </div>

    {# Комментарии #}
    <div class="bg-white rounded-md shadow p-4">
        <div class="flex items-center justify-between">
            <div class="p-2 bg-yellow-100 ">
                <svg class="w-5 h-5 text-yellow-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"></path>
                </svg>
            </div>
            {% if stats.comments_today > 0 %}
            <span class="text-xs font-medium text-green-600 bg-green-100 px-2 py-0.5 rounded-full">+{{ stats.comments_today }} today</span>
            {% endif %}
        </div>
        <p class="text-2xl font-bold text-gray-900 mt-2">{{ stats.total_comments|number_format }}</p>
        <p class="text-xs text-gray-500">Комментарии</p>
    </div>

    {# Модели #}
    <div class="bg-white rounded-md shadow p-4">
        <div class="flex items-center justify-between">
            <div class="p-2 bg-purple-100 ">
                <svg class="w-5 h-5 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11.049 2.927c.3-.921 1.603-.921 1.902 0l1.519 4.674a1 1 0 00.95.69h4.915c.969 0 1.371 1.24.588 1.81l-3.976 2.888a1 1 0 00-.363 1.118l1.518 4.674c.3.922-.755 1.688-1.538 1.118l-3.976-2.888a1 1 0 00-1.176 0l-3.976 2.888c-.783.57-1.838-.197-1.538-1.118l1.518-4.674a1 1 0 00-.363-1.118l-3.976-2.888c-.784-.57-.38-1.81.588-1.81h4.914a1 1 0 00.951-.69l1.519-4.674z"></path>
                </svg>
            </div>
        </div>
        <p class="text-2xl font-bold text-gray-900 mt-2">{{ stats.total_models|number_format }}</p>
        <p class="text-xs text-gray-500">Модели</p>
    </div>

    {# Лайки #}
    <div class="bg-white rounded-md shadow p-4">
        <div class="flex items-center justify-between">
            <div class="p-2 bg-red-100 ">
                <svg class="w-5 h-5 text-red-600" fill="currentColor" viewBox="0 0 24 24">
                    <path d="M12 21.35l-1.45-1.32C5.4 15.36 2 12.28 2 8.5 2 5.42 4.42 3 7.5 3c1.74 0 3.41.81 4.5 2.09C13.09 3.81 14.76 3 16.5 3 19.58 3 22 5.42 22 8.5c0 3.78-3.4 6.86-8.55 11.54L12 21.35z"></path>
                </svg>
            </div>
        </div>
        <p class="text-2xl font-bold text-gray-900 mt-2">{{ stats.total_likes|number_format }}</p>
        <p class="text-xs text-gray-500">Лайки</p>
    </div>
</div>

{# Статусы видео #}
<div class="grid grid-cols-2 md:grid-cols-4 gap-4 mb-6">
    <div class="bg-white rounded-md shadow p-4 border-l-4 border-green-500">
        <div class="flex items-center justify-between">
            <div>
                <p class="text-sm text-gray-500">Опубликовано</p>
                <p class="text-xl font-bold text-gray-900">{{ stats.videos_published|number_format }}</p>
            </div>
            <span class="text-green-500 text-2xl">✓</span>
        </div>
    </div>
    
    <div class="bg-white rounded-md shadow p-4 border-l-4 border-blue-500">
        <div class="flex items-center justify-between">
            <div>
                <p class="text-sm text-gray-500">В обработке</p>
                <p class="text-xl font-bold text-gray-900">{{ stats.videos_processing|number_format }}</p>
            </div>
            <span class="text-blue-500 text-2xl">⟳</span>
        </div>
    </div>
    
    <div class="bg-white rounded-md shadow p-4 border-l-4 border-gray-500">
        <div class="flex items-center justify-between">
            <div>
                <p class="text-sm text-gray-500">Черновики</p>
                <p class="text-xl font-bold text-gray-900">{{ stats.videos_draft|number_format }}</p>
            </div>
            <span class="text-gray-500 text-2xl">📝</span>
        </div>
    </div>
    
    <div class="bg-white rounded-md shadow p-4 border-l-4 border-yellow-500">
        <div class="flex items-center justify-between">
            <div>
                <p class="text-sm text-gray-500">Приватные</p>
                <p class="text-xl font-bold text-gray-900">{{ stats.videos_private|number_format }}</p>
            </div>
            <span class="text-yellow-500 text-2xl">🔒</span>
        </div>
    </div>
</div>

{# Графики статистики #}
<div class="grid grid-cols-1 lg:grid-cols-3 gap-6 mb-6">
    <div class="bg-white rounded-md shadow p-6">
        <h3 class="text-sm font-semibold text-gray-900 mb-4">📈 Загрузки видео (30 дней)</h3>
        {{ component('stats_chart', {type: 'uploads', days: 30}) }}
    </div>
    <div class="bg-white rounded-md shadow p-6">
        <h3 class="text-sm font-semibold text-gray-900 mb-4">👥 Новые пользователи (30 дней)</h3>
        {{ component('stats_chart', {type: 'users', days: 30}) }}
    </div>
    <div class="bg-white rounded-md shadow p-6">
        <h3 class="text-sm font-semibold text-gray-900 mb-4">👁 Просмотры (30 дней)</h3>
        {{ component('stats_chart', {type: 'views', days: 30}) }}
    </div>
</div>

{# Storage Dashboard Widget #}
{% if storage_stats is defined and storage_stats|length > 0 %}
<div class="bg-white rounded-md shadow mb-6">
    <div class="px-6 py-4 border-b flex items-center justify-between">
        <div>
            <h3 class="text-sm font-semibold text-gray-900">Хранилища</h3>
            <p class="text-xs text-gray-500">Использование дискового пространства</p>
        </div>
        <a href="{{ path('admin_storage') }}" class="text-blue-600 hover:text-blue-800 text-xs">
            Управление →
        </a>
    </div>
    <div class="p-4">
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
            {% for key, stat in storage_stats %}
            <div class="border  p-3 {% if stat.warningExceeded %}border-orange-300 bg-orange-50{% else %}border-gray-200{% endif %}">
                <div class="flex items-center justify-between mb-2">
                    <div class="flex items-center">
                        <span class="text-lg mr-2">
                            {% if stat.type == 'local' %}💾
                            {% elseif stat.type == 'ftp' %}📡
                            {% elseif stat.type == 'sftp' %}🔐
                            {% elseif stat.type == 'http' %}🌐
                            {% else %}📦{% endif %}
                        </span>
                        <span class="font-medium text-gray-900 text-sm">{{ stat.name }}</span>
                    </div>
                    <span class="text-xs px-2 py-0.5 rounded-full 
                        {% if stat.type == 'local' %}bg-gray-100 text-gray-600
                        {% elseif stat.type == 'ftp' %}bg-blue-100 text-blue-600
                        {% elseif stat.type == 'sftp' %}bg-green-100 text-green-600
                        {% elseif stat.type == 'http' %}bg-purple-100 text-purple-600
                        {% else %}bg-gray-100 text-gray-600{% endif %}">
                        {{ stat.type|upper }}
                    </span>
                </div>
                <div class="text-xs text-gray-600">
                    <span class="font-semibold text-gray-900">{{ stat.filesCount }}</span> файлов • 
                    <span class="font-semibold text-gray-900">{{ stat.totalSizeFormatted }}</span>
                </div>
            </div>
            {% endfor %}
        </div>
    </div>
</div>
{% endif %}

{# Виджеты: Очередь обработки, Ошибки, Активность #}
<div class="grid grid-cols-1 lg:grid-cols-3 gap-6 mb-6">
    {# Очередь обработки #}
    <div class="bg-white rounded-md shadow">
        <div class="px-6 py-4 border-b">
            <h3 class="text-sm font-semibold text-gray-900">⚙️ Очередь обработки</h3>
            <p class="text-xs text-gray-500">Статус worker</p>
        </div>
        <div class="p-6">
            <div class="space-y-4">
                <div class="flex items-center justify-between">
                    <span class="text-sm text-gray-600">В очереди:</span>
                    <span class="text-lg font-bold text-blue-600">{{ stats.queue_stats.total }}</span>
                </div>
                <div class="flex items-center justify-between">
                    <span class="text-sm text-gray-600">Обрабатывается:</span>
                    <span class="text-lg font-bold text-yellow-600">{{ stats.queue_stats.processing }}</span>
                </div>
                <div class="flex items-center justify-between">
                    <span class="text-sm text-gray-600">Черновики:</span>
                    <span class="text-lg font-bold text-gray-600">{{ stats.queue_stats.draft }}</span>
                </div>
            </div>
            <div class="mt-4 pt-4 border-t">
                {% set worker_status = worker_status() %}
                {% if worker_status.running %}
                <div class="flex items-center text-xs text-green-600">
                    <span class="w-2 h-2 rounded-full bg-green-500 animate-pulse mr-2"></span>
                    Worker активен (PID: {{ worker_status.pid }})
                </div>
                {% else %}
                <div class="flex items-center text-xs text-red-600">
                    <span class="w-2 h-2 rounded-full bg-red-500 mr-2"></span>
                    Worker остановлен
                </div>
                {% endif %}
            </div>
        </div>
    </div>

    {# Последние ошибки #}
    <div class="bg-white rounded-md shadow">
        <div class="px-6 py-4 border-b flex items-center justify-between">
            <div>
                <h3 class="text-sm font-semibold text-gray-900">❌ Ошибки обработки</h3>
                <p class="text-xs text-gray-500">Последние проблемы</p>
            </div>
            {% if recent_errors|length > 0 %}
            <span class="text-xs px-2 py-0.5 rounded-full bg-red-100 text-red-800">{{ recent_errors|length }}</span>
            {% endif %}
        </div>
        <div class="p-4">
            {% if recent_errors|length > 0 %}
            <div class="space-y-2">
                {% for video in recent_errors %}
                <div class="text-xs border-l-2 border-red-500 pl-2 py-1">
                    <a href="{{ path('admin_videos_edit', {id: video.id}) }}" class="text-gray-900 hover:text-blue-600 font-medium block truncate">
                        {{ video.title|length > 30 ? video.title|slice(0, 30) ~ '...' : video.title }}
                    </a>
                    <span class="text-gray-500">{{ video.updatedAt|date('d.m.Y H:i') }}</span>
                </div>
                {% endfor %}
            </div>
            {% else %}
            <div class="text-center py-8 text-gray-500 text-sm">
                <svg class="w-12 h-12 mx-auto mb-2 text-green-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
                Нет ошибок
            </div>
            {% endif %}
        </div>
    </div>

    {# Активность пользователей #}
    <div class="bg-white rounded-md shadow">
        <div class="px-6 py-4 border-b">
            <h3 class="text-sm font-semibold text-gray-900">👥 Последние пользователи</h3>
            <p class="text-xs text-gray-500">Недавно зарегистрированные</p>
        </div>
        <div class="p-4">
            {% if recent_activity|length > 0 %}
            <div class="space-y-2">
                {% for user in recent_activity|slice(0, 5) %}
                <div class="flex items-center text-xs">
                    {% if user.avatar %}
                    <img src="{{ asset('media/avatars/' ~ user.avatar) }}" alt="{{ user.username }}" class="w-6 h-6 rounded-full mr-2">
                    {% else %}
                    <div class="w-6 h-6 rounded-full mr-2 bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center text-white text-xs font-bold">
                        {{ user.username|slice(0, 1)|upper }}
                    </div>
                    {% endif %}
                    <div class="flex-1 min-w-0">
                        <p class="text-gray-900 font-medium truncate">{{ user.username }}</p>
                        <p class="text-gray-500">{{ user.createdAt|date('d.m.Y H:i') }}</p>
                    </div>
                </div>
                {% endfor %}
            </div>
            {% else %}
            <div class="text-center py-8 text-gray-500 text-sm">
                Нет пользователей
            </div>
            {% endif %}
        </div>
    </div>
</div>

{# Последние видео #}
<div class="bg-white rounded-md shadow">
    <div class="px-6 py-4 border-b flex items-center justify-between">
        <div>
            <h3 class="text-sm font-semibold text-gray-900">Последние видео</h3>
            <p class="text-xs text-gray-500">Недавно добавленные</p>
        </div>
        <a href="{{ path('admin_videos') }}" class="text-blue-600 hover:text-blue-800 text-xs">
            Все видео →
        </a>
    </div>
    <div class="overflow-x-auto">
        <table class="min-w-full divide-y divide-gray-200">
            <thead class="bg-gray-50">
                <tr>
                    <th class="px-4 py-2 text-left text-xs font-medium text-gray-500 uppercase">Постер</th>
                    <th class="px-4 py-2 text-left text-xs font-medium text-gray-500 uppercase">Название</th>
                    <th class="px-4 py-2 text-left text-xs font-medium text-gray-500 uppercase">Статус</th>
                    <th class="px-4 py-2 text-left text-xs font-medium text-gray-500 uppercase">Дата</th>
                </tr>
            </thead>
            <tbody class="bg-white rounded-md divide-y divide-gray-200">
                {% for video in recent_videos %}
                <tr class="hover:bg-gray-50">
                    <td class="px-4 py-2">
                        <img src="{{ video_poster(video.poster) }}" alt="{{ video.title }}" class="w-16 h-10 object-cover rounded">
                    </td>
                    <td class="px-4 py-2">
                        <a href="{{ path('admin_videos_edit', {id: video.id}) }}" class="text-sm text-gray-900 hover:text-blue-600">
                            {{ video.title|length > 40 ? video.title|slice(0, 40) ~ '...' : video.title }}
                        </a>
                        <div class="text-xs text-gray-500">{{ video.durationFormatted }}</div>
                    </td>
                    <td class="px-4 py-2">
                        <span class="inline-flex px-2 py-0.5 text-xs font-medium rounded-full
                            {% if video.status == 'published' %}bg-green-100 text-green-800
                            {% elseif video.status == 'processing' %}bg-blue-100 text-blue-800
                            {% elseif video.status == 'pending' %}bg-yellow-100 text-yellow-800
                            {% elseif video.status == 'failed' %}bg-red-100 text-red-800
                            {% else %}bg-gray-100 text-gray-800{% endif %}">
                            {{ video.status }}
                        </span>
                    </td>
                    <td class="px-4 py-2 text-xs text-gray-500">
                        {{ video.createdAt|date('d.m.Y H:i') }}
                    </td>
                </tr>
                {% else %}
                <tr>
                    <td colspan="4" class="px-4 py-8 text-center text-gray-500 text-sm">
                        Нет видео
                    </td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>
{% endblock %}
