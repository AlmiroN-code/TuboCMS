{% extends 'admin/base.html.twig' %}

{% block title %}Результаты A/B теста: {{ test.name }}{% endblock %}
{% block page_title %}Результаты A/B теста{% endblock %}

{% block content %}
<div class="mb-6">
    <div class="flex items-center justify-between">
        <div>
            <h1 class="text-2xl font-bold text-gray-900">{{ test.name }}</h1>
            <p class="mt-1 text-sm text-gray-500">{{ test.description }}</p>
        </div>
        <div class="flex items-center space-x-3">
            {% set statusColors = {
                'draft': 'bg-gray-100 text-gray-800',
                'running': 'bg-green-100 text-green-800',
                'paused': 'bg-yellow-100 text-yellow-800',
                'completed': 'bg-blue-100 text-blue-800'
            } %}
            <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium {{ statusColors[test.status]|default('bg-gray-100 text-gray-800') }}">
                {{ test.status|upper }}
            </span>
            <a href="{{ path('admin_ads_abtests_edit', {id: test.id}) }}" 
               class="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-md text-sm transition">
                Редактировать тест
            </a>
        </div>
    </div>
</div>

{# Информация о тесте #}
<div class="grid grid-cols-1 md:grid-cols-4 gap-4 mb-6">
    <div class="bg-white p-4 rounded-md shadow">
        <div class="text-sm font-medium text-gray-500">Период теста</div>
        <div class="mt-1 text-lg font-semibold text-gray-900">
            {% if test.startDate %}
                {{ test.startDate|date('d.m.Y') }}
            {% else %}
                Не задано
            {% endif %}
            —
            {% if test.endDate %}
                {{ test.endDate|date('d.m.Y') }}
            {% else %}
                ∞
            {% endif %}
        </div>
    </div>
    
    <div class="bg-white p-4 rounded-md shadow">
        <div class="text-sm font-medium text-gray-500">Распределение трафика</div>
        <div class="mt-1 text-lg font-semibold text-gray-900">
            {{ test.trafficSplitA }}% / {{ test.trafficSplitB }}%
        </div>
    </div>
    
    <div class="bg-white p-4 rounded-md shadow">
        <div class="text-sm font-medium text-gray-500">Метрика победителя</div>
        <div class="mt-1 text-lg font-semibold text-gray-900">
            {{ test.winnerMetric|upper }}
        </div>
    </div>
    
    <div class="bg-white p-4 rounded-md shadow">
        <div class="text-sm font-medium text-gray-500">Статус</div>
        <div class="mt-1 text-lg font-semibold text-gray-900">
            {% if test.status == 'running' %}
                <span class="text-green-600">Активен</span>
            {% elseif test.status == 'completed' %}
                <span class="text-blue-600">Завершен</span>
            {% elseif test.status == 'paused' %}
                <span class="text-yellow-600">Приостановлен</span>
            {% else %}
                <span class="text-gray-600">{{ test.status|title }}</span>
            {% endif %}
        </div>
    </div>
</div>

{# Сравнение вариантов #}
<div class="grid grid-cols-1 lg:grid-cols-2 gap-6 mb-6">
    {# Вариант A #}
    <div class="bg-white rounded-md shadow">
        <div class="px-6 py-4 border-b border-gray-200 bg-blue-50">
            <h3 class="text-lg font-medium text-gray-900 flex items-center">
                <span class="bg-blue-600 text-white rounded-full w-6 h-6 flex items-center justify-center text-sm font-bold mr-3">A</span>
                {% if variantA %}
                    {{ variantA.name }}
                {% else %}
                    Вариант не выбран
                {% endif %}
            </h3>
        </div>
        <div class="p-6">
            {% if variantA and statsA %}
                <div class="space-y-4">
                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <div class="text-sm font-medium text-gray-500">Показы</div>
                            <div class="text-2xl font-bold text-gray-900">{{ statsA.impressions|number_format(0, '.', ' ') }}</div>
                        </div>
                        <div>
                            <div class="text-sm font-medium text-gray-500">Клики</div>
                            <div class="text-2xl font-bold text-gray-900">{{ statsA.clicks|number_format(0, '.', ' ') }}</div>
                        </div>
                    </div>
                    
                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <div class="text-sm font-medium text-gray-500">CTR</div>
                            <div class="text-2xl font-bold text-blue-600">
                                {% if statsA.impressions > 0 %}
                                    {{ ((statsA.clicks / statsA.impressions) * 100)|number_format(2) }}%
                                {% else %}
                                    0%
                                {% endif %}
                            </div>
                        </div>
                        <div>
                            <div class="text-sm font-medium text-gray-500">Конверсии</div>
                            <div class="text-2xl font-bold text-gray-900">{{ statsA.conversions|default(0)|number_format(0, '.', ' ') }}</div>
                        </div>
                    </div>
                    
                    {% if statsA.revenue is defined %}
                        <div>
                            <div class="text-sm font-medium text-gray-500">Доход</div>
                            <div class="text-2xl font-bold text-green-600">{{ statsA.revenue|number_format(2) }} ₽</div>
                        </div>
                    {% endif %}
                </div>
            {% else %}
                <div class="text-center py-8 text-gray-500">
                    <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path>
                    </svg>
                    <p class="mt-2">Нет данных для отображения</p>
                </div>
            {% endif %}
        </div>
    </div>

    {# Вариант B #}
    <div class="bg-white rounded-md shadow">
        <div class="px-6 py-4 border-b border-gray-200 bg-green-50">
            <h3 class="text-lg font-medium text-gray-900 flex items-center">
                <span class="bg-green-600 text-white rounded-full w-6 h-6 flex items-center justify-center text-sm font-bold mr-3">B</span>
                {% if variantB %}
                    {{ variantB.name }}
                {% else %}
                    Вариант не выбран
                {% endif %}
            </h3>
        </div>
        <div class="p-6">
            {% if variantB and statsB %}
                <div class="space-y-4">
                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <div class="text-sm font-medium text-gray-500">Показы</div>
                            <div class="text-2xl font-bold text-gray-900">{{ statsB.impressions|number_format(0, '.', ' ') }}</div>
                        </div>
                        <div>
                            <div class="text-sm font-medium text-gray-500">Клики</div>
                            <div class="text-2xl font-bold text-gray-900">{{ statsB.clicks|number_format(0, '.', ' ') }}</div>
                        </div>
                    </div>
                    
                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <div class="text-sm font-medium text-gray-500">CTR</div>
                            <div class="text-2xl font-bold text-green-600">
                                {% if statsB.impressions > 0 %}
                                    {{ ((statsB.clicks / statsB.impressions) * 100)|number_format(2) }}%
                                {% else %}
                                    0%
                                {% endif %}
                            </div>
                        </div>
                        <div>
                            <div class="text-sm font-medium text-gray-500">Конверсии</div>
                            <div class="text-2xl font-bold text-gray-900">{{ statsB.conversions|default(0)|number_format(0, '.', ' ') }}</div>
                        </div>
                    </div>
                    
                    {% if statsB.revenue is defined %}
                        <div>
                            <div class="text-sm font-medium text-gray-500">Доход</div>
                            <div class="text-2xl font-bold text-green-600">{{ statsB.revenue|number_format(2) }} ₽</div>
                        </div>
                    {% endif %}
                </div>
            {% else %}
                <div class="text-center py-8 text-gray-500">
                    <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"></path>
                    </svg>
                    <p class="mt-2">Нет данных для отображения</p>
                </div>
            {% endif %}
        </div>
    </div>
</div>

{# Сравнительная таблица и выводы #}
{% if statsA and statsB %}
    <div class="bg-white rounded-md shadow mb-6">
        <div class="px-6 py-4 border-b border-gray-200">
            <h3 class="text-lg font-medium text-gray-900">Сравнительный анализ</h3>
        </div>
        <div class="p-6">
            <div class="overflow-x-auto">
                <table class="min-w-full divide-y divide-gray-200">
                    <thead class="bg-gray-50">
                        <tr>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Метрика</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Вариант A</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Вариант B</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Разница</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Победитель</th>
                        </tr>
                    </thead>
                    <tbody class="bg-white divide-y divide-gray-200">
                        <tr>
                            <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900">CTR</td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                                {% set ctrA = statsA.impressions > 0 ? (statsA.clicks / statsA.impressions) * 100 : 0 %}
                                {{ ctrA|number_format(2) }}%
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                                {% set ctrB = statsB.impressions > 0 ? (statsB.clicks / statsB.impressions) * 100 : 0 %}
                                {{ ctrB|number_format(2) }}%
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm">
                                {% set ctrDiff = ctrB - ctrA %}
                                {% if ctrDiff > 0 %}
                                    <span class="text-green-600">+{{ ctrDiff|number_format(2) }}%</span>
                                {% elseif ctrDiff < 0 %}
                                    <span class="text-red-600">{{ ctrDiff|number_format(2) }}%</span>
                                {% else %}
                                    <span class="text-gray-500">0%</span>
                                {% endif %}
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm">
                                {% if ctrB > ctrA %}
                                    <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                                        Вариант B
                                    </span>
                                {% elseif ctrA > ctrB %}
                                    <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-blue-100 text-blue-800">
                                        Вариант A
                                    </span>
                                {% else %}
                                    <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-gray-100 text-gray-800">
                                        Равны
                                    </span>
                                {% endif %}
                            </td>
                        </tr>
                        <tr>
                            <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900">Клики</td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">{{ statsA.clicks|number_format(0, '.', ' ') }}</td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">{{ statsB.clicks|number_format(0, '.', ' ') }}</td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm">
                                {% set clicksDiff = statsB.clicks - statsA.clicks %}
                                {% if clicksDiff > 0 %}
                                    <span class="text-green-600">+{{ clicksDiff|number_format(0, '.', ' ') }}</span>
                                {% elseif clicksDiff < 0 %}
                                    <span class="text-red-600">{{ clicksDiff|number_format(0, '.', ' ') }}</span>
                                {% else %}
                                    <span class="text-gray-500">0</span>
                                {% endif %}
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm">
                                {% if statsB.clicks > statsA.clicks %}
                                    <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                                        Вариант B
                                    </span>
                                {% elseif statsA.clicks > statsB.clicks %}
                                    <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-blue-100 text-blue-800">
                                        Вариант A
                                    </span>
                                {% else %}
                                    <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-gray-100 text-gray-800">
                                        Равны
                                    </span>
                                {% endif %}
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    {# Рекомендации #}
    <div class="bg-white rounded-md shadow">
        <div class="px-6 py-4 border-b border-gray-200">
            <h3 class="text-lg font-medium text-gray-900">Рекомендации</h3>
        </div>
        <div class="p-6">
            {% if ctrB > ctrA %}
                <div class="bg-green-50 border border-green-200 rounded-md p-4">
                    <div class="flex">
                        <div class="flex-shrink-0">
                            <svg class="h-5 w-5 text-green-400" fill="currentColor" viewBox="0 0 20 20">
                                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
                            </svg>
                        </div>
                        <div class="ml-3">
                            <h3 class="text-sm font-medium text-green-800">Вариант B показывает лучшие результаты</h3>
                            <div class="mt-2 text-sm text-green-700">
                                <p>Рекомендуется переключить весь трафик на вариант B. CTR выше на {{ ctrDiff|number_format(2) }}%.</p>
                            </div>
                        </div>
                    </div>
                </div>
            {% elseif ctrA > ctrB %}
                <div class="bg-blue-50 border border-blue-200 rounded-md p-4">
                    <div class="flex">
                        <div class="flex-shrink-0">
                            <svg class="h-5 w-5 text-blue-400" fill="currentColor" viewBox="0 0 20 20">
                                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
                            </svg>
                        </div>
                        <div class="ml-3">
                            <h3 class="text-sm font-medium text-blue-800">Вариант A показывает лучшие результаты</h3>
                            <div class="mt-2 text-sm text-blue-700">
                                <p>Рекомендуется оставить вариант A. CTR выше на {{ (ctrA - ctrB)|number_format(2) }}%.</p>
                            </div>
                        </div>
                    </div>
                </div>
            {% else %}
                <div class="bg-yellow-50 border border-yellow-200 rounded-md p-4">
                    <div class="flex">
                        <div class="flex-shrink-0">
                            <svg class="h-5 w-5 text-yellow-400" fill="currentColor" viewBox="0 0 20 20">
                                <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd"></path>
                            </svg>
                        </div>
                        <div class="ml-3">
                            <h3 class="text-sm font-medium text-yellow-800">Результаты равны</h3>
                            <div class="mt-2 text-sm text-yellow-700">
                                <p>Оба варианта показывают одинаковые результаты. Рекомендуется продолжить тест или выбрать вариант с меньшими затратами на производство.</p>
                            </div>
                        </div>
                    </div>
                </div>
            {% endif %}
        </div>
    </div>
{% endif %}

<div class="mt-6 flex justify-start">
    <a href="{{ path('admin_ads_abtests') }}" 
       class="bg-gray-600 hover:bg-gray-700 text-white px-4 py-2 rounded-md transition">
        ← Назад к списку тестов
    </a>
</div>
{% endblock %}