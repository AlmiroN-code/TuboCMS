{% extends 'admin/base.html.twig' %}

{% block title %}Управление кэшем{% endblock %}
{% block page_title %}Управление кэшем{% endblock %}

{% block content %}
<div class="grid grid-cols-1 lg:grid-cols-3 gap-6 mb-6">
    {# Статистика кэша #}
    <div class="bg-white rounded-md shadow p-6">
        <div class="flex items-center">
            <div class="p-3 bg-blue-100 rounded-md">
                <span class="text-2xl">💾</span>
            </div>
            <div class="ml-4">
                <p class="text-sm font-medium text-gray-600">Размер кэша</p>
                <p class="text-2xl font-semibold text-gray-900">{{ cache_size }}</p>
            </div>
        </div>
    </div>
    
    <div class="bg-white rounded-md shadow p-6">
        <div class="flex items-center">
            <div class="p-3 bg-green-100 rounded-md">
                <span class="text-2xl">🌍</span>
            </div>
            <div class="ml-4">
                <p class="text-sm font-medium text-gray-600">Окружение</p>
                <p class="text-2xl font-semibold text-gray-900">{{ environment }}</p>
            </div>
        </div>
    </div>
    
    <div class="bg-white rounded-md shadow p-6">
        <div class="flex items-center">
            <div class="p-3 bg-purple-100 rounded-md">
                <span class="text-2xl">📁</span>
            </div>
            <div class="ml-4">
                <p class="text-sm font-medium text-gray-600">Пулов кэша</p>
                <p class="text-2xl font-semibold text-gray-900">{{ pools|length }}</p>
            </div>
        </div>
    </div>
</div>

<div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
    {# Быстрые действия #}
    <div class="bg-white rounded-md shadow">
        <div class="px-6 py-4 border-b">
            <h3 class="text-lg font-semibold text-gray-900">⚡ Быстрые действия</h3>
        </div>
        <div class="p-6 space-y-4">
            <form action="{{ path('admin_cache_clear', {pool: 'all'}) }}" method="post" class="inline">
                <input type="hidden" name="_token" value="{{ csrf_token('cache_clear') }}">
                <button type="submit" 
                        onclick="return confirm('Очистить весь кэш?')"
                        class="w-full flex items-center justify-between px-4 py-3 bg-red-50 hover:bg-red-100 text-red-700 rounded-md transition">
                    <span class="flex items-center gap-2">
                        <span>🗑️</span>
                        <span class="font-medium">Очистить весь кэш</span>
                    </span>
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                    </svg>
                </button>
            </form>
            
            <form action="{{ path('admin_cache_clear', {pool: 'settings'}) }}" method="post" class="inline">
                <input type="hidden" name="_token" value="{{ csrf_token('cache_clear') }}">
                <button type="submit" 
                        class="w-full flex items-center justify-between px-4 py-3 bg-yellow-50 hover:bg-yellow-100 text-yellow-700 rounded-md transition">
                    <span class="flex items-center gap-2">
                        <span>⚙️</span>
                        <span class="font-medium">Очистить кэш настроек</span>
                    </span>
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                    </svg>
                </button>
            </form>
            
            <form action="{{ path('admin_cache_warmup') }}" method="post" class="inline">
                <input type="hidden" name="_token" value="{{ csrf_token('cache_warmup') }}">
                <button type="submit" 
                        class="w-full flex items-center justify-between px-4 py-3 bg-green-50 hover:bg-green-100 text-green-700 rounded-md transition">
                    <span class="flex items-center gap-2">
                        <span>🔥</span>
                        <span class="font-medium">Прогреть кэш</span>
                    </span>
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                    </svg>
                </button>
            </form>
        </div>
    </div>
    
    {# Пулы кэша #}
    <div class="bg-white rounded-md shadow">
        <div class="px-6 py-4 border-b">
            <h3 class="text-lg font-semibold text-gray-900">🗄️ Пулы кэша</h3>
        </div>
        <div class="divide-y">
            {% for pool_id, description in pools %}
            <div class="px-6 py-4 flex items-center justify-between">
                <div>
                    <p class="font-medium text-gray-900">{{ pool_id }}</p>
                    <p class="text-sm text-gray-500">{{ description }}</p>
                </div>
                <form action="{{ path('admin_cache_clear', {pool: pool_id}) }}" method="post">
                    <input type="hidden" name="_token" value="{{ csrf_token('cache_clear') }}">
                    <button type="submit" 
                            class="px-3 py-1 text-sm bg-gray-100 hover:bg-gray-200 text-gray-700 rounded transition">
                        Очистить
                    </button>
                </form>
            </div>
            {% endfor %}
        </div>
    </div>
</div>

{# Информация #}
<div class="mt-6 bg-blue-50 border border-blue-200 rounded-md p-4">
    <h4 class="font-semibold text-blue-900 mb-2">💡 Информация о кэше</h4>
    <ul class="text-sm text-blue-800 space-y-1">
        <li>• <strong>Директория кэша:</strong> <code class="bg-blue-100 px-1 rounded">{{ cache_dir }}</code></li>
        <li>• Кэш настроек автоматически обновляется при изменении настроек в админке</li>
        <li>• Рекомендуется очищать кэш после обновления кода или конфигурации</li>
        <li>• Прогрев кэша загружает часто используемые данные заранее</li>
    </ul>
</div>
{% endblock %}
