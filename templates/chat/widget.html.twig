<div class="bg-white rounded-lg shadow-lg flex flex-col {{ roomId starts with 'live-' ? 'h-[600px]' : 'h-full' }}" 
     data-controller="chat" 
     data-chat-room-id-value="{{ roomId }}"
     data-chat-poll-interval-value="3000">
    
    {# Заголовок чата #}
    <div class="px-4 py-3 border-b border-gray-200 bg-gray-50 rounded-t-lg">
        <div class="flex items-center justify-between">
            <h3 class="font-semibold text-gray-900">💬 Чат</h3>
            <span class="text-xs text-gray-500">{{ onlineCount|default(0) }} онлайн</span>
        </div>
    </div>

    {# Область сообщений #}
    <div class="flex-1 overflow-y-auto p-4 space-y-2" 
         data-chat-target="messages"
         style="{{ roomId starts with 'live-' ? '' : 'max-height: 400px; min-height: 300px;' }}">
        {# Сообщения загружаются динамически #}
    </div>

    {# Форма отправки #}
    {% if app.user %}
        <div class="px-4 py-3 border-t border-gray-200 bg-gray-50 rounded-b-lg">
            <form data-chat-target="form" data-action="submit->chat#submit" class="flex gap-2">
                <input type="text" 
                       data-chat-target="input"
                       placeholder="Написать сообщение..."
                       maxlength="1000"
                       class="flex-1 px-3 py-2 border border-gray-300 rounded-md text-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                       required>
                <button type="submit" 
                        class="px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700 transition-colors text-sm font-medium">
                    Отправить
                </button>
            </form>
        </div>
    {% else %}
        <div class="px-4 py-3 border-t border-gray-200 bg-gray-50 rounded-b-lg text-center">
            <p class="text-sm text-gray-600">
                <a href="{{ path('app_login') }}" class="text-blue-600 hover:text-blue-700 font-medium">Войдите</a>, чтобы писать в чат
            </p>
        </div>
    {% endif %}
</div>
